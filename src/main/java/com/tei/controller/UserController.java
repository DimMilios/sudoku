package com.tei.controller;

/**
 * The interface User controller contains methods for dealing with
 * the model representing a game user.
 */
public interface UserController {

	/**
	 * Update user model with the given username.
	 *
	 * @param username the username to be set for user model
	 */
	void updateUserModelWith(String username);

}
